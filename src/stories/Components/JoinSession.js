import React from "react";

import { storiesOf } from "@storybook/react";

import JoinSession from "../../Components/JoinSession";

storiesOf("Join session", module).add("Join session", () => <JoinSession />);
