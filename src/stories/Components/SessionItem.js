import React from "react";

import { storiesOf } from "@storybook/react";

import SessionItem from "../../Components/SessionItem";

storiesOf("Session Item", module).add("Session item", () => <SessionItem />);
